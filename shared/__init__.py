"""Shared libraries that span the workflow engine and the adhoc system.

Phase 0: empty package. Phase 3 lands `customer_call_audit.py` here — the
single source of truth for the per-customer daily call cap across both
schedulers.
"""
