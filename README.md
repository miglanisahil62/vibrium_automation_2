# vibrium-workflow

Node-based, live-editable collections-call workflow engine. Runs at arm's length from the adhoc `vibrium-automation` system; shares only the compliance library and a per-customer call-audit table.

See `PHASES.md` for the build plan.
See `~/.claude/plans/this-is-for-vibrium-wiggly-puffin.md` for the architecture decisions.

**Status:** scaffolding (Phase 0).
